//Library for standard input and output like: printf(), scanf(), fgets(), etc.
#include <stdio.h>
//Standard library to with general utility functions as such: malloc(), free(), exit(), atoi(), etc.
#include <stdlib.h>
//Library for String operations like: strlen(), strcpy(), strtok(), strcmp(), etc.
#include <string.h>
//Unix standard API with functions/system calls like: fork(), exec(), pipe(), sleep(), etc.
#include <unistd.h>
//Library for data type declaration like: pid_t
#include <sys/types.h>
//API for process mangement like: wait()
#include <sys/wait.h>

int main() {
    return 0;
}

